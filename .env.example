# Core TicketWatcher configuration
OPENAI_API_KEY=
GITHUB_TOKEN=
ALLOWED_PATHS=
TICKETWATCHER_TRIGGER_LABELS=agent-fix,auto-pr
TICKETWATCHER_MODEL=gpt-4o-mini
MAX_FILES=4
MAX_LINES=200
DEFAULT_AROUND_LINES=60

# Optional GitHub metadata overrides for local runs
GITHUB_REPOSITORY=
GITHUB_WORKSPACE=

# Cloudflare Worker secrets (used by ticketwatcher-worker)
CF_ACCOUNT_ID=
CF_API_TOKEN=
GITHUB_WEBHOOK_SECRET=
